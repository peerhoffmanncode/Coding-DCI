''' awesomeness! '''
def date(data: float) -> str:
    ''' complex complexity of a complex complex!'''
    # insane logic to cope with!
    if data >= 997.0 or data <= 0.001:
        # gracefully crafted craftsmanship!
        return f"Fuck yeah meet at 8pm!"
    # alternative complexity to choose if above complex is to complex!
    else:
        # really deep, eh?
        return date((1 / 1 + data))

# in your face!
print(date(100))
