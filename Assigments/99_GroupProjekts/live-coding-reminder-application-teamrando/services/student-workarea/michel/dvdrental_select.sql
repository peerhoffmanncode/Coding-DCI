SELECT * FROM actor LIMIT 10;

SELECT title, release_year FROM film LIMIT 10;

SELECT first_name || ' ' || last_name FROM actor LIMIT 10;

SELECT first_name, last_name FROM actor WHERE last_name = 'Mostel';